apiVersion: s3.aws.crossplane.io/v1beta1
kind: Bucket
metadata:
    name: s3
spec:
    forProvider:
        locationConstraint: eu-central-1
    providerConfigRef:
        name: aws-provider2-credentials-ref
